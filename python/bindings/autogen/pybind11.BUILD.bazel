# -*- python -*-

licenses(["notice"])  # BSD-3-Clause

package(default_visibility = ["//visibility:public"])

cc_library(
    name = "pybind11",
    hdrs = native.glob(["include/pybind11/**/*.h"]),
    includes = ["include"],
    deps = ["@python"],
)
